# 🔥 Firebase 設定指南

語音通話功能需要 Firebase Realtime Database 來同步在線用戶狀態，這樣即使使用不同網路（4G/5G）的用戶也能看到彼此在線。

## 📋 設定步驟（5 分鐘完成）

### 1️⃣ 建立 Firebase 專案

1. 前往 **Firebase Console**：https://console.firebase.google.com/
2. 點擊「**新增專案**」或「**Add project**」
3. 輸入專案名稱，例如：`kyoto-travel-voice`
4. （可選）啟用 Google Analytics
5. 點擊「**建立專案**」

### 2️⃣ 啟用 Realtime Database

1. 在 Firebase Console 左側選單，點擊「**Realtime Database**」
2. 點擊「**建立資料庫**」
3. 選擇資料庫位置（建議選擇 `asia-southeast1` 新加坡，離台灣較近）
4. 安全性規則選擇「**以測試模式啟動**」（暫時允許所有讀寫）
5. 點擊「**啟用**」

### 3️⃣ 設定安全性規則（重要！）

在 Realtime Database 頁面，切換到「**規則**」標籤，貼上以下規則：

```json
{
  "rules": {
    "presence": {
      "$uid": {
        ".read": true,
        ".write": true
      }
    }
  }
}
```

點擊「**發布**」。

> 💡 這個規則允許所有人讀取在線用戶列表，但每個用戶只能修改自己的狀態。

### 4️⃣ 獲取 Firebase 配置

1. 在 Firebase Console 點擊左上角的 ⚙️ 圖示 → 「**專案設定**」
2. 向下捲動找到「**你的應用程式**」區塊
3. 如果還沒有應用程式，點擊「**</>**」（網頁）圖示新增應用程式
4. 輸入應用程式暱稱（例如：`Kyoto Travel Web`）
5. **不需要**勾選 Firebase Hosting
6. 點擊「**註冊應用程式**」
7. 你會看到類似下面的 `firebaseConfig` 物件：

```javascript
const firebaseConfig = {
  apiKey: "AIzaSyC1234567890abcdefghijklmnop",
  authDomain: "kyoto-travel-voice.firebaseapp.com",
  databaseURL: "https://kyoto-travel-voice-default-rtdb.asia-southeast1.firebasedatabase.app",
  projectId: "kyoto-travel-voice",
  storageBucket: "kyoto-travel-voice.appspot.com",
  messagingSenderId: "123456789012",
  appId: "1:123456789012:web:abcdef1234567890"
};
```

### 5️⃣ 更新專案配置

1. 開啟 `src/firebase.config.js`
2. 將 `firebaseConfig` 物件的內容替換成你自己的配置
3. **特別注意 `databaseURL`** 這個欄位必須正確

### 6️⃣ 完成！

儲存檔案後：
```bash
npm run build
git add -A
git commit -m "add firebase config"
git push origin main
```

等待 GitHub Actions 部署完成（約 2-3 分鐘），就可以在手機上測試跨網路的在線用戶發現功能了！

## 🎯 測試方式

1. 在手機 A（4G）打開 APP
2. 點擊 📞 → 設定名稱（例如：爸爸）
3. 在手機 B（5G）打開 APP  
4. 點擊 📞 → 設定名稱（例如：媽媽）
5. 切換到「🔍 發現」標籤
6. 應該會看到對方出現在列表中！

## 💰 費用說明

**完全免費！**

Firebase Spark（免費方案）包含：
- ✅ 1GB 儲存空間
- ✅ 10GB/月 下載量
- ✅ 100 個同時連線

對於家庭語音通話 APP 來說完全夠用，不會產生任何費用。

## 🔐 安全性說明

目前的規則允許任何人查看在線用戶，這對家庭使用是可以接受的。如果想要更高的安全性：

1. 啟用 Firebase Authentication（匿名登入）
2. 修改規則要求驗證才能讀寫
3. 加入家庭群組概念（只顯示同一群組的成員）

需要的話我可以幫你實現更安全的版本！

## ❓ 常見問題

### Q1: 我沒看到「Realtime Database」選項？
**A:** 確認你選擇的是「Realtime Database」而非「Firestore Database」，兩者是不同的產品。

### Q2: databaseURL 是什麼格式？
**A:** 格式為 `https://YOUR-PROJECT-ID-default-rtdb.REGION.firebasedatabase.app`
例如：`https://kyoto-travel-voice-default-rtdb.asia-southeast1.firebasedatabase.app`

### Q3: 部署後看不到其他人在線？
**A:** 
1. 檢查 Firebase 配置是否正確
2. 打開瀏覽器開發者工具（F12）查看 Console 是否有錯誤
3. 確認兩個裝置都已經設定好名稱
4. 確認 Firebase 規則已正確設定

### Q4: 可以看到在線用戶的數量限制嗎？
**A:** 免費方案支援 100 個同時連線，對家庭使用綽綽有餘。

---

**需要協助？** 把錯誤訊息或問題告訴我，我會幫你解決！
